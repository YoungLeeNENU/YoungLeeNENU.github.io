import React, { Component } from 'react';
import './minibuffer-editor.css';

class MinibufferEditor extends Component {
    constructor (prop) {
        super(prop);
    }

    render () {
        return (
            <div className="minibuffer-editor"></div>
        );
    }
}

export default MinibufferEditor;
